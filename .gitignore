# Terraform
.terraform/
.terraform.lock.hcl  # Optional: many ignore this too, but committing it is good for consistency

# Terraform state
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Overrides
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Variables (don't commit secrets!)
*.tfvars
*.tfvars.json

# Modules (if downloaded)
.terraform/modules/